{%- comment -%}
  MOONDAE — Bestsellers Section
{%- endcomment -%}

{%- assign collection = section.settings.collection -%}

<section class="bestsellers-section">
  <div class="container">
    <div class="section-header">
      <div>
        <h2 class="section-title">{{ section.settings.heading }}</h2>
        <p class="section-sub">{{ section.settings.subheading }}</p>
      </div>
      {%- if collection != blank -%}
        <a href="{{ collection.url }}" class="see-all">Lihat Semua</a>
      {%- endif -%}
    </div>

    <div class="product-grid">
      {%- if collection != blank -%}
        {%- for product in collection.products limit: section.settings.limit -%}
          {% render 'product-card', product: product %}
        {%- endfor -%}
      {%- else -%}
        {%- for i in (1..3) -%}
          {% render 'product-card-placeholder', index: i %}
        {%- endfor -%}
      {%- endif -%}
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Bestseller",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Judul",
      "default": "Bestseller"
    },
    {
      "type": "text",
      "id": "subheading",
      "label": "Subjudul",
      "default": "Yang paling banyak dicintai"
    },
    {
      "type": "collection",
      "id": "collection",
      "label": "Koleksi"
    },
    {
      "type": "range",
      "id": "limit",
      "label": "Jumlah produk",
      "min": 3,
      "max": 8,
      "step": 1,
      "default": 3
    }
  ],
  "presets": [
    {
      "name": "Bestseller"
    }
  ]
}
{% endschema %}
