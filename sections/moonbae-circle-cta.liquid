{%- comment -%}
  MOONDAE — Moonbae Circle Newsletter CTA Strip
{%- endcomment -%}

<section class="moonbae-strip">
  <div class="container">
    <div class="moonbae-strip-inner">
      <div class="moonbae-text">
        <p class="eyebrow">{{ section.settings.eyebrow }}</p>
        <h2>{{ section.settings.heading }}</h2>
        <p>{{ section.settings.description }}</p>
        <div class="moonbae-benefits">
          {%- for block in section.blocks -%}
            <span class="moonbae-benefit" {{ block.shopify_attributes }}>{{ block.settings.text }}</span>
          {%- endfor -%}
        </div>
      </div>

      <div class="moonbae-form">
        {%- form 'customer', id: 'moonbae-newsletter' -%}
          <input type="hidden" name="contact[tags]" value="moonbae-circle,newsletter">
          <div class="email-form">
            <input
              type="email"
              name="contact[email]"
              placeholder="{{ section.settings.placeholder }}"
              required
              autocomplete="email"
            >
            <button type="submit">{{ section.settings.button_text }}</button>
          </div>
          {%- if form.posted_successfully? -%}
            <p style="color: var(--moondae-gold); font-size: 0.875rem; margin-top: 8px;">
              Welcome to Moonbae Circle! &#10022;
            </p>
          {%- endif -%}
        {%- endform -%}
        <p style="font-size: 0.75rem; color: rgba(255,255,255,0.4); margin-top: 8px;">
          {{ section.settings.disclaimer }}
        </p>
      </div>
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Moonbae Circle CTA",
  "settings": [
    {
      "type": "text",
      "id": "eyebrow",
      "label": "Label atas",
      "default": "MOONBAE CIRCLE"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Judul",
      "default": "Join Moonbae Circle"
    },
    {
      "type": "textarea",
      "id": "description",
      "label": "Deskripsi",
      "default": "Join a community of women who appreciate beauty in simplicity. Get early access, member discounts, and weekly styling content."
    },
    {
      "type": "text",
      "id": "placeholder",
      "label": "Placeholder email",
      "default": "Your email address"
    },
    {
      "type": "text",
      "id": "button_text",
      "label": "Teks tombol",
      "default": "Join"
    },
    {
      "type": "text",
      "id": "disclaimer",
      "label": "Disclaimer",
      "default": "By signing up, you agree to our privacy policy. Unsubscribe anytime."
    }
  ],
  "blocks": [
    {
      "type": "benefit",
      "name": "Benefit",
      "settings": [
        {
          "type": "text",
          "id": "text",
          "label": "Teks benefit",
          "default": "Early access"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Moonbae Circle CTA",
      "blocks": [
        { "type": "benefit", "settings": { "text": "Early access" } },
        { "type": "benefit", "settings": { "text": "10% first discount" } },
        { "type": "benefit", "settings": { "text": "Weekly styling tips" } },
        { "type": "benefit", "settings": { "text": "Exclusive events" } }
      ]
    }
  ]
}
{% endschema %}
